[project]
name = "visual-operation"
version = "0.1.0"
description = "Hand tracking visual operation app"
requires-python = ">=3.12"
dependencies = [
    "mediapipe>=0.10.0",
    "opencv-contrib-python>=4.8.0",
    "numpy>=1.24.0",
]

[tool.uv]
override-dependencies = [
    "opencv-python",
]
